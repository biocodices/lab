<%= bootstrap_form_for(@dna_sample) do |f| %>
  <% if @dna_sample.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dna_sample.errors.count, "error") %> prohibited this dna_sample from being saved:</h2>

      <ul>
      <% @dna_sample.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label class="control-label">Extracted from this sample:</label><br>
    <%= collection_select(:dna_sample, :sample_id, Sample.all.decorate,
                          :id, :description, {},
                          {class: "form-control inline"}) %>
  </div>
  <div class="field">
    <%= f.date_select :extraction_date %>
  </div>
  <div class="field">
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
