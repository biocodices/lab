<%= form_for(@dna_sample) do |f| %>
  <% if @dna_sample.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dna_sample.errors.count, "error") %> prohibited this dna_sample from being saved:</h2>

      <ul>
      <% @dna_sample.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sample_id %><br>
    <%= f.text_field :sample_id %>
  </div>
  <div class="field">
    <%= f.label :old_id %><br>
    <%= f.text_field :old_id %>
  </div>
  <div class="field">
    <%= f.label :extraction_date %><br>
    <%= f.date_select :extraction_date %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
