<h1 class="title">
  <%= fa_icon 'braille' %>
  Editing Library #<%= @library.id %> (<span id="counter"><%= @library.dna_extractions.count %></span>)
</h1>

<%= render partial: 'form', 
    locals: { submit_button_text: 'Update the library info' } %>
