<table class='table table-striped table-condensed'>
  <thead>
    <tr>
      <th>DNA Tag</th>
      <th>Patient name</th>
      <th>Well</th>
      <th>i5</th>
      <th>i7</th>
    </tr>
  </thead>

  <tbody>
    <% library_dnas.each do |library_dna| %>

      <tr class="display-row collapsed" data-toggle="collapse" data-target="#dna-detail-<%= library_dna.id %><%= "-#{owner_id}" if defined? owner_id %>" class="accordion-toggle">
        <td><%= library_dna.dna_extraction.tag %></td>
        <td><%= library_dna.patient.full_name %></td>
        <td><%= library_dna.well %></td>
        <td><%= library_dna.i5 %></td>
        <td><%= library_dna.i7 %></td>
      </tr>

      <tr class="hidden-row">
        <td colspan="5">
          <div class="accordion-body collapse" id="dna-detail-<%= library_dna.id %><%= "-#{owner_id}" if defined? owner_id %>">

            <%= render library_dna.dna_extraction.decorate %>

          </div><!-- accordion-body -->
        </td>
      </tr>

    <% end %>
  </tbody>
</table>

